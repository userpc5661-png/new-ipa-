org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# The user-selected project folder contains an en dash. Gradle's default
# Windows path guard rejects it even though the toolchain supports the path.
android.overridePathCheck=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
org.gradle.caching=true
org.gradle.parallel=true
# Building this OneDrive project through a short substituted drive keeps
# native tooling stable, but Kotlin incremental caches cannot cross drive
# roots (project on Q:, pub cache on C:). Full compilation is deterministic.
kotlin.incremental=false
